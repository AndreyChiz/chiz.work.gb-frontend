export default function WrapperPricing({ children }) {

    return (
        <section section className="site-section bg-light" id="pricing-section" >
            <div className="container">
                {children}
            </div>
        </section>

    )

}