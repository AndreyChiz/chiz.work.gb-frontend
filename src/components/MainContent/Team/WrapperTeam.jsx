export default function WrapperTeam({ children }) {
    return (
        <div className="site-section" id="team-section">
            <div className="container">
                {children}
            </div>
        </div>
    )
}